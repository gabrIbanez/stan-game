# Copier vers .env.production avant : docker compose -f docker-compose.prod.yml --env-file .env.production up -d --build

# Port exposé sur la machine (reverse proxy HTTPS en amont recommandé)
APP_PORT=3000

POSTGRES_USER=postgres
POSTGRES_PASSWORD=changez-moi-en-production
POSTGRES_DB=stan_game
